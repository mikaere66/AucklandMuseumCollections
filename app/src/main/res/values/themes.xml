<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AucklandMuseumV3API" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- For pre Android 12 devices, icon is plain black OR white,
        depending on system's dark mode. See also values-night -->
    <style name="Theme.AucklandMuseumV3API.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_animator</item>
        <item name="windowSplashScreenAnimationDuration">@integer/splash_duration</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.AucklandMuseumV3API</item>
    </style>

</resources>